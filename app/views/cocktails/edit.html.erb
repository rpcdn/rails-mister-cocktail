<h1>Edit data about <%= @cocktail.name %></h1>

<%= simple_form_for [@cocktail] do |f| %>
  <%= f.input :name %>
  <%= f.input :photos, as: :attachinary %>
  <%= f.submit "Update cocktail", class: "btn btn-primary" %>
<% end %>
